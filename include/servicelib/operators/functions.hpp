#pragma once

// Go analog: operators/functions.go. In C++ operator functions are represented
// by StreamFunction and constrained callables instead of one interface per
// operator, so the shared definitions live in runtime/function.hpp.
#include <servicelib/runtime/function.hpp>
